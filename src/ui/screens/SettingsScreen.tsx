import { ScreenContainer } from '../components/layout/ScreenContainer';

export function SettingsScreen() {
  return (
    <ScreenContainer
      title="Settings"
      subtitle="Application settings and preferences are intentionally left as placeholders."
    />
  );
}
